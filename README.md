# Music-Recommendation-System
A full‑stack application that recommends songs based on semantic similarity of embeddings. Built with Node.js, Express, MongoDB, and React, it leverages GenAI embeddings + cosine similarity to match user queries with the most relevant tracks.
